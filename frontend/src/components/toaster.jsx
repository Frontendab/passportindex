import { Toaster } from "react-hot-toast";

const ToasterNotify = () => {
    return (
        <Toaster
            position="top-right"
            reverseOrder={false}
        />
    )
};

export default ToasterNotify;